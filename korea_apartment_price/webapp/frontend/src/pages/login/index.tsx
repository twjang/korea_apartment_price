import * as MUI from '@material-ui/core';
import { PageConfig } from "../../misc/routeConfig";

export const cfg: PageConfig = {
  path: "/login",
  title: "로그인",
  isLoginPage: true,
  element: (<></>)
}